{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the TRACE main window is granted. Kept minimal and widened only when a milestone genuinely needs it.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "dialog:default", "os:default", "fs:default"]
}
